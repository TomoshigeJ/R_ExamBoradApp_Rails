<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1><%= t('.title')%></h1>
      <% if @board.errors.any? %>
        <%= render 'shared/errors_message', object: @board %>
      <% end %>
      <%= render 'form', { board: @board } %>
    </div>
  </div>
</div>